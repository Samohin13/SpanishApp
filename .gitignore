*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.claude/settings.local.json

# Release signing
keystore.properties
*.keystore
*.jks
.kotlin/

# Собранные релизы — не коммитим (артефакты воспроизводимы из тегов; 41 МБ каждый).
/app/release/
*.aab

# google-services.json ОСТАЁТСЯ в трекинге: он нужен для сборки (плагин
# com.google.gms.google-services без него падает), а Firebase Android-ключи —
# не секреты (SHA-ограничены + Security Rules). Не игнорируем.
tts_diag.log
